# Backend API URL (used when not proxied through Vite dev server)
# VITE_API_URL=http://localhost:3001
# Cloud template API URL (self-hosted only; leave empty to use VITE_API_URL)
# VITE_TEMPLATE_API_URL=https://app.qwenweaver.com
